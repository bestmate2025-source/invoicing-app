@extends('layouts.app') @section('title',$client->name) @section('content')<div class="d-flex justify-content-between"><h1>{{ $client->name }}</h1><div><a class="btn btn-outline-secondary" href="{{ route('clients.edit',$client) }}">Edit</a> <form class="d-inline" method="POST" action="{{ route('clients.destroy',$client) }}">@csrf @method('DELETE')<button class="btn btn-outline-danger" onclick="return confirm('Delete this client?')">Delete</button></form></div></div><p>{{ $client->company }} · {{ $client->email }} · {{ $client->phone }}</p><hr><h4>Invoices</h4><table class="table"><tr><th>Number</th><th>Date</th><th>Total</th><th>Status</th></tr>@forelse($client->invoices as $invoice)<tr><td><a href="{{ route('invoices.show',$invoice) }}">{{ $invoice->invoice_number }}</a></td><td>{{ $invoice->issue_date->format('M d, Y') }}</td><td>${{ number_format($invoice->total,2) }}</td><td>{{ ucfirst($invoice->status) }}</td></tr>@empty<tr><td colspan="4">No invoices.</td></tr>@endforelse</table>@endsection
