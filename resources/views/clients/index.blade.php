@extends('layouts.app') @section('title','Clients') @section('content')<div class="d-flex justify-content-between mb-3"><h1>Clients</h1><a class="btn btn-primary" href="{{ route('clients.create') }}">+ Add client</a></div><div class="card"><table class="table table-hover mb-0"><tr><th>Name</th><th>Company</th><th>Email</th><th>Invoices</th><th></th></tr>@forelse($clients as $client)<tr><td><a href="{{ route('clients.show',$client) }}">{{ $client->name }}</a></td><td>{{ $client->company ?: '—' }}</td><td>{{ $client->email ?: '—' }}</td><td>{{ $client->invoices_count }}</td><td><a class="btn btn-sm btn-outline-secondary" href="{{ route('clients.edit',$client) }}">Edit</a></td></tr>@empty<tr><td colspan="5" class="text-center p-4">No clients yet.</td></tr>@endforelse</table></div>{{ $clients->links() }} @endsection
