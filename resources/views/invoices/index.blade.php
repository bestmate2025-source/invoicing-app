@extends('layouts.app') @section('title','Invoices') @section('content')<div class="d-flex justify-content-between mb-3"><h1>Invoices</h1><a class="btn btn-primary" href="{{ route('invoices.create') }}">+ New invoice</a></div><div class="card"><table class="table table-hover mb-0"><tr><th>Number</th><th>Client</th><th>Issue date</th><th>Total</th><th>Status</th></tr>@forelse($invoices as $invoice)<tr><td><a href="{{ route('invoices.show',$invoice) }}">{{ $invoice->invoice_number }}</a></td><td>{{ $invoice->client->name }}</td><td>{{ $invoice->issue_date->format('M d, Y') }}</td><td>${{ number_format($invoice->total,2) }}</td><td><span class="badge text-bg-{{ $invoice->status==='paid'?'success':($invoice->status==='partial'?'warning':'secondary') }}">{{ ucfirst($invoice->status) }}</span></td></tr>@empty<tr><td colspan="5" class="text-center p-4">No invoices yet.</td></tr>@endforelse</table></div>{{ $invoices->links() }} @endsection
